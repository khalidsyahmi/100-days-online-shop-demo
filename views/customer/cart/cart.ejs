<%- include('../../shared/includes/head', {dynamicTitle: 'My Cart'}) %>
<link rel="stylesheet" href="/styles/cart.css">

</head>
<body>
    <%- include('../../shared/includes/nav-bar-header') %>
    <main>
        <h1>My Cart</h1>        
        <ul id="cart-items">
            <% for (const cartItem of locals.cart.items) { %>
                <li>
                    <%- include('includes/cart-item',{ item: cartItem }) %>
                </li>
            <% } %>
        </ul>
        <div id="cart-total">
            <p> Total: $<%= locals.cart.totalPrice.toFixed(2) %></p>
            <button class="btn">Checkout Items</button>
        </div>
    </main>
<%- include('../../shared/includes/footer') %>
